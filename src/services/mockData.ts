import { comprehensiveServiceCatalog } from './comprehensiveServiceCatalog';

/**
 * Export comprehensive catalog with 30+ AWS services
 * This replaces the original 5-service mock data
 */
export const mockServiceCatalog = comprehensiveServiceCatalog;
